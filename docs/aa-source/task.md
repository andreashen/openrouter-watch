# Artificial Analysis 直连任务清单

## 阶段 1：映射身份依据

- [x] group 与 `rules[].manual_override` 对齐：`ref` + `note`，并覆盖每个成员
- [x] validator 拒绝无依据、未覆盖、`exact_slug` group、快照字段缺失

## 阶段 2：抓取与快照

- [x] 分页不变量失败时不写快照、连续成功清零
- [x] 三项分数只读当前快照

## 阶段 3：公开闸门

- [x] G0 未通过时展示与停采都保持原状
- [x] 提交的开关文件和主榜源码不切到 AA
- [x] 版本对齐函数区分评测页、文档当前版本和接口信封

## 依赖关系

映射校验不依赖实时 API。快照感知校验在有快照时追加。公开切流依赖 G0–G6 全部为真，本阶段不打开。
